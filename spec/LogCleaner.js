/** class LogCleaner */
class LogCleaner extends MonitoredObject {
    constructor( config ) {
        super( config );
        console.log( "constructing LogCleaner..." )
    }

    // select the object string

    // convert it to a json object

    // clear the log object array

    // restringify

    // update the database

}
